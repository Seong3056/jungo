# === Django & Core ===
Django>=5.0.6,<6
djangorestframework>=3.15,<4
Pillow>=10.0
python-dotenv>=1.0

# === Channels (WebSocket) ===
channels>=4.0,<5
channels-redis>=4.1,<5

# === Static files & ASGI Server ===
whitenoise>=6.6
daphne>=4.1
